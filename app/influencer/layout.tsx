import { ReactNode } from 'react';

export const metadata = {
  title: 'Influencer Dashboard | AfriMatch',
  description: 'Track your referrals, earnings, and performance metrics as an AfriMatch influencer partner',
};

export default function InfluencerLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
